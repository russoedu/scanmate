/** Each signature checked against the bytes it covers. */

export type { SealReport, SignatureCheck, SignatureProblem, Signer } from './seal-report.contract'
export { verifySignatures } from './verify-signatures.use-case'
