/** The room around a marked region where ink still belongs to it. */

export type { Bleed, ResolvedBleed } from './bleed.contract'
export { DEFAULT_BLEED, growBy, hasBleed, resolveBleed, resolveRegionBleed } from './resolve-bleed.policy'
