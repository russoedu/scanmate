/** Which boxes are ticked, on the original and on the returned scan, read on each side's own terms. */

export { checkGroups } from './checkbox-group.policy'
export type { Checkbox, CheckboxGroup, CheckboxOptions, CheckboxReading, CheckboxSide, CheckboxState, GroupReading } from './checkbox.contract'
export { checkboxesFromMasks, readCheckboxes } from './read-checkboxes.use-case'
