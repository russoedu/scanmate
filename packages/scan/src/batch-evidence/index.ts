/** A long document's evidence as one PDF: one cover for the whole of it, then every batch's sheets. */

export type { AuditingBatchSession, BatchEvidence, BatchEvidenceOptions } from './batch-evidence.contract'
export { writeBatchEvidence } from './write-batch-evidence.use-case'
