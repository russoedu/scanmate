/** Whether required content is on each page of the scan, and where the original puts it. */

export { findContent } from './find-content.use-case'
export type { ContentResult, ExpectedContent, FindOptions, FindReport, FoundBy, Occurrence, PageFind } from './find-report.contract'
