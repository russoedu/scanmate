/** A page's text layer: its text, and every run of it placed on the displayed page. */

export { readTextLayer } from './read-text-layer.use-case'
export type { TextLayer } from './read-text-layer.use-case'
